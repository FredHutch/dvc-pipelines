params {
    sc {
        scanpy {
            batch_effect_correct {
                method = 'mnncorrect'
                report_ipynb = "${params.misc.test.enabled ? '../../..' : ''}/lib/modules/scanpy/bin/reports/sc_mnncorrect_report.ipynb"
                off = 'h5ad'
            }
        }
    }
}
